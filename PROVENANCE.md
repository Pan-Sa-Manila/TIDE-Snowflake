# Dataset & Licence Provenance

Per Official Rules §4.3(b): every dataset used in the Entry is listed here with its source and licence.

---

## Datasets

| Dataset | Source | Licence |
|---|---|---|
| `TIDE.RETAIL.*` (orders, items, payments, refunds, shipments, tracking, stock) | **Synthetic.** Hand-authored in `sql/seed/`, generated per `docs/DETAILS.md` §15.4. All names, SKUs, carriers, and amounts are invented; emails use the reserved `example.com` domain (RFC 2606) | N/A (original work) |
| `TIDE.DECISION.POLICIES` | **Synthetic.** Policy texts authored for this project | N/A (original work) |
| `TIDE.DECISION.RULE_CONSTANTS` | **Synthetic.** Business constants per `docs/DETAILS.md` §6 | N/A (original work) |
| `TIDE.DECISION.REASON_COPY` | **Synthetic.** Customer-facing reason text per `docs/DETAILS.md` §12 | N/A (original work) |
| Proof / demo images | Photographed or generated by the team for this project — no scraped or third-party images | N/A (original work) |

**No Snowflake Marketplace listings, external APIs, or third-party datasets are consumed.**

---

## Models (via Snowflake Cortex)

All models are invoked through Snowflake Cortex inside the account; no external AI endpoints.

| Purpose | Cortex Feature | Model |
|---|---|---|
| Text (structured output) | `AI_COMPLETE` | Configured in `DECISION.RULE_CONSTANTS.TEXT_MODEL` at deploy time |
| Vision (proof analysis) | `AI_COMPLETE` | Configured in `DECISION.RULE_CONSTANTS.VISION_MODEL` at deploy time |
| Agent orchestration | Cortex Agent (`DATA_AGENT_RUN`) | `auto` (Cortex-selected) |
| Policy retrieval | Cortex Search | N/A (embedding-based) |
| Data queries | Cortex Analyst (semantic view) | N/A (Cortex-managed) |

---

## Tooling

| Tool | Usage | Licence |
|---|---|---|
| **CoCo CLI** | Build-time AI assistant; transcripts in `evidence/coco-transcripts/` | Snowflake |
| **Snowflake CLI** | SQL execution, procedure upload, app deployment | Snowflake |
| **Streamlit in Snowflake** | Entire UI (warehouse runtime) | Snowflake |
| **Snowpark Python** | Stored procedure runtime | Snowflake |
| **pytest** | Decision engine tests (local, no account needed) | MIT |
| **Pydantic** | Input validation in procedures | MIT |

---

## Code Provenance

- All application code in this repository is original work by the team.
- No code was copied from external repositories, tutorials, or code-generation outputs without attribution.
- CoCo CLI build sessions are logged as `stream-json` transcripts in `evidence/coco-transcripts/` for full transparency.

---

*Update this file if anything above changes before submission — it is part of the Entry.*
